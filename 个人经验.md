# 环境与运行备注

这份文件只记录本仓库的本地操作经验，不是锁定环境，也不是实验结果证明。

## 推荐顺序

1. 从 GitHub 完整源码仓库开始：

       git clone --recurse-submodules https://github.com/zy8389/EvidenceTrack-GS.git
       cd EvidenceTrack-GS

2. 先安装 requirements-cpu.txt，运行 compileall、pytest、smoke 和 synthetic recovery。
3. 真实 GPU 运行按 research_docs/FERN_PRECHECK_AND_RUN.md 的 fail-closed 顺序执行。
4. 保存实际环境报告、checkpoint provenance、cache hash 和 pair audit；不要只保存终端截图。

## 环境记录

environment.yml 是一个可复用起点，不等于已验证的 CUDA 组合。真实运行必须把 python tools/collect_environment.py 的输出和 GPU/CUDA 编译信息保存到 run 目录。不同机器上的 PyTorch、CUDA、驱动和编译器差异都应在实验记录中明确写出。

## 失败处理

如果启用的几何约束或正则化初始化/forward 失败，训练会立即退出，并在输出目录写入 run_status.json，状态为 INVALID。不要把这种 run 当成 baseline，也不要手工把零 loss 的日志改成通过。
