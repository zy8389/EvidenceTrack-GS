# EvidenceTrack-GS：实验需求与执行清单

本文档是当前完整源码仓库的实验前检查清单。它不代表任何尚未运行的结果，也不承诺性能优越性。

## 1. 获取源码与依赖

    git clone --recurse-submodules https://github.com/zy8389/EvidenceTrack-GS.git
    cd EvidenceTrack-GS
    export PYTHONPATH="$PWD"

当前仓库将 submodules/diff-gaussian-rasterization-confidence/ 和 submodules/simple-knn/ 作为已跟踪源码分发，而不是依赖一个未提交的 Git submodule 工作树。--recurse-submodules 保留给未来依赖；当前 clone 后这两个目录应直接存在。

CPU 检查依赖见 requirements-cpu.txt。真实 CUDA 环境必须记录 Python、PyTorch、CUDA、编译器和两个扩展的实际版本，不能把未验证的环境文件当成成功证明。

## 2. CPU 门禁

    pip install -r requirements-cpu.txt
    python -m compileall -q diffusion_guidance geometric_constraints tools tests arguments scene gaussian_renderer train.py utils
    python -m pytest -q tests
    python tools/smoke_test_phase2_1.py
    python tools/test_geometry_recovery.py --synthetic-smoke --output validation/synthetic_recovery.json

预期是所有命令零状态退出，synthetic JSON 中 passed=true。FakeDifix 只用于 CPU 协议测试，不代表真实模型推理。

## 3. 真实实验前置条件

真实 Fern run 必须依次通过：

    environment
    -> tracks
    -> projection
    -> pseudo-live
    -> CUDA geometry smoke
    -> A0 @ 10k
    -> A1 / SelfRender / B @ 12k
    -> pair audits
    -> metrics and identity diagnostics

运行前准备 LLFF 图像与相机文件、固定的 source-only Track H5、CUDA 扩展和独立 Difix/DINOv2 环境。完整命令见 research_docs/FERN_PRECHECK_AND_RUN.md。

## 4. 有效性规则

- 启用的 geometry constraint 或 regularizer 发生异常时，训练必须停止并写 run_status.json: INVALID。
- 失败 Track、失败相机投影、失败 pseudo cache、checkpoint provenance 不一致或 pair audit 失败的 run 不得进入汇总。
- 结果表必须保留失败样本和 NR，不能删除不利样本后再计算均值。
- Fern development 结果不能冒充跨场景 confirmatory 结果。

## 5. 许可与引用

源码和第三方组件受各自许可约束，尤其是 Gaussian Splatting 研究/非商业许可。使用前阅读根目录 LICENSE.md 和 THIRD_PARTY_NOTICES.md；本文件不授予额外许可。
