# 完整待实验列表

共62项，包括历史CPU门禁记录与未执行服务器实验。当前主LLFF JSON/CSV矩阵为96项运行：开发24项、确认72项；每个scene/seed的A0只训练一次，A1、SelfRender、B都从同一A0续训，不是四个独立10k训练。工作簿是此前72条矩阵的历史快照，执行以JSON/CSV为准。本轮修复后的测试依用户要求尚未运行，旧11/23日志不能验证当前源码。后续DTU/Mip360、外部基线及未实现的控制驱动另列，不能当作已提供完整复现。

## G01｜历史11项smoke快照与当前重跑（P0）

问题：基础strict / evidence / schedule能否运行。前置：无。入口：scripts/run_cpu.sh。

验收：历史快照为11/11；当前源码须重新运行；Mock不是实际Difix。实现：已有CPU测试入口；运行：当前源码NOT RUN。证据：validation/smoke_phase21.log（历史源码快照）。

## G02｜历史23项regression快照与当前重跑（P0）

问题：坐标、尺度、配对和checkpoint局部实现。前置：无。入口：pytest tests -q。

验收：历史快照为23/23；当前源码须重新运行；不替代GPU集成。实现：已有CPU测试入口；运行：当前源码NOT RUN。证据：validation/regression_tests.log（历史源码快照）。

## G03｜单点合成恢复（P0）

问题：多相机源约束能否恢复扰动点。前置：无。入口：test_geometry_recovery.py --synthetic-smoke。

验收：历史快照曾记录9.7511→0.0276px；当前源码须重跑；只验证单点。实现：已有CPU测试入口；运行：当前源码NOT RUN。证据：validation/synthetic_recovery.json（历史源码快照）。

## G04｜完整上游装配（P0）

问题：固定SHA与累计补丁能否真实装配。前置：无。入口：scripts/bootstrap.sh。

验收：禁止fuzzy；保存git diff及revision marker。实现：已有入口；运行：待服务器。证据：NR。

## G05｜CUDA扩展编译（P0）

问题：本仓库rasterizer/simple-knn接口一致。前置：G04。入口：教程第3节。

验收：导入成功；保存torch/CUDA/compiler版本。实现：已有入口；运行：待服务器。证据：NR。

## G06｜实际相机投影一致（P0）

问题：renderer与Track是否同一像素坐标。前置：G05;G08。入口：run_stage.sh projection。

验收：original/training分辨率均须max error≤1e-4px；JSON须passed=true且输入指纹未陈旧。实现：已有fail-closed服务器门禁；运行：待服务器。证据：NR。

## G07｜源图尺寸与去畸变（P0）

问题：像素坐标是否对应给定校准。前置：G04。入口：数据检查。

验收：camera模型和RGB尺寸一致；不默默忽略distortion。实现：人工数据门禁；运行：待服务器。证据：NR。

## G08｜Strict membership审计（P0）

问题：只使用source-source匹配。前置：G05;G07。入口：run_stage.sh tracks。

验收：无full points3D membership；最少32条；覆盖报告、H5 hash和JSON passed=true。实现：已有入口；运行：待服务器。证据：NR。

## G09｜held-out替换等价（P0）

问题：目标图是否间接影响训练。前置：G08。入口：删除/替换held-out后比较source store与短训。

验收：训练张量/源采样相同；评估标签允许变化。实现：待加完整训练对照驱动；运行：待服务器。证据：NR。

## G10｜真实Gaussian梯度（P0）

问题：Track loss是否作用于当前μ。前置：G06;G08。入口：run_stage.sh smoke。

验收：投影与live pseudo门禁当前有效；有限非零梯度；空loss不得成功；写JSON passed=true。实现：已有入口；运行：待服务器。证据：NR。

## G11｜零监督与退化失败（P0）

问题：是否隐藏无效几何。前置：G10。入口：pytest与人工退化数据。

验收：零权重/退化信息明确失败；不记为好结果。实现：局部测试已有，真实场景待跑；运行：待服务器。证据：NR。

## G12｜真实checkpoint恢复（P0）

问题：Adam与辅助状态是否完整。前置：G10。入口：A0→复制状态→短续跑比对。

验收：v2格式；旧tuple拒绝；最终更新后存储。实现：CPU fixture已有，GPU驱动待补；运行：待服务器。证据：NR。

## G13｜更新后评测一致（P0）

问题：日志与保存的PLY/checkpoint是否同一步。前置：G12。入口：重载final checkpoint独立渲染。

验收：PSNR/SSIM/LPIPS与更新后日志一致。实现：待加重载一致性脚本；运行：待服务器。证据：NR。

## G14｜真实Difix重复推理（P0）

问题：actual model是否达到像素容差。前置：G05;G21。入口：run_stage.sh cache。

验收：重复推理；记录精确/容差模式和模型hash。实现：已有入口；运行：待服务器。证据：NR。

## G15｜缓存版本与画幅（P0）

问题：是否混旧图或改坐标。前置：G14。入口：run_stage.sh cache。

验收：输入/参考/输出hash、model revision/code commit、分辨率、32camera唯一且reproducibility_check.passed=true。实现：已有入口；运行：待服务器。证据：NR。

## G16｜实际A1/SelfRender/B条件配对（P0）

问题：训练分支是否只改变指定的pseudo target条件。前置：G24;G25;G30。入口：run_stage.sh pair-self; pair-b; pair-pseudo。

验收：同起点hash、同2000 source序列；伪监督分支均39calls/32views；SelfRender/B camera keys与schedule完全相同；每个scene/seed报告passed=true。实现：已有入口；运行：待服务器。证据：NR。

## G17｜多尺度真实数据复跑（P0）

问题：局部尺度不变能否延伸实际工程。前置：G10。入口：场景与translation共同×10。

验收：报告quality差、梯度和输出差；不要求逐位同。实现：待加数据变换驱动；运行：待服务器。证据：NR。

## G18｜代理对应标签审计（P0）

问题：SIFT heldout标签有多少误配。前置：G08。入口：每场景固定随机抽100条人工核验。

验收：可见/遮挡/重复纹理分层；发布核验规则。实现：人工标注任务；运行：待服务器。证据：NR。

## G19｜代码与权重锁（P0）

问题：所有依赖是否可追溯。前置：G05;G14。入口：collect_environment.py＋sha256sum。

验收：DINO权重也需hash；环境记录归档。实现：已有入口；运行：待服务器。证据：NR。

## G20｜匿名与许可证（P0）

问题：能否作为匿名补充材料。前置：全部关键实验。入口：人工发布前审计。

验收：保留第三方许可；移除私有路径和身份。实现：投稿前人工审计；运行：待实验。证据：NR。

## G21｜开发场景A0（P1）

问题：构建共同10k基线。前置：G10。入口：run_stage.sh A0。

验收：fern/room；source RGB+track；记录全部配置。实现：已有入口；运行：待服务器。证据：NR。

## G22｜实际anchor恢复（P1）

问题：已训练高斯可否受轨迹纠正。前置：G21。入口：run_stage.sh recover。

验收：0.005/0.01/0.02尺度扰动；保留失败；只读原ckpt。实现：已有入口；运行：待服务器。证据：NR。

## G23｜固定32视图导出（P1）

问题：cache是否仅由共同A0产生。前置：G21。入口：run_stage.sh export; pseudo-audit。

验收：source参考；恰好32个唯一camera fingerprints；pose/intrinsics/provenance及manifest hash门禁passed=true。实现：已有入口；运行：待服务器。证据：NR。

## G24｜A1正常2k续训（P1）

问题：单纯更多训练带来多少改善。前置：G21。入口：run_stage.sh A1。

验收：与SelfRender/B相同起点和source序列、冻结拓扑。实现：已有入口；运行：待服务器。证据：NR。

## G25｜B冻结Difix2k续训（P1）

问题：在相同 pseudo-view 监督下，用冻结 Difix target 替换冻结 A0 self-render target 是否额外有效；主 target 替换对照是 B−SelfRender，B−A1仅表示总分支效应，扩散机制独有性仍需G31。前置：G14;G23;G30。入口：run_stage.sh B。

验收：39次call；32unique；pseudo camera、调用序列、loss和权重与SelfRender一致，起点与source序列同时和A1一致。实现：已有入口；运行：待服务器。证据：NR。

## G26｜开发三seed重复（P1）

问题：观察收益方向是否稳定。前置：G16。入口：seeds 1,2,3重复G21-G25及G30。

验收：fern/room仅作开发，不用于确认性结论。实现：已有入口；运行：待服务器。证据：NR。

## G27｜LLFF确认性六场景（P1）

问题：未调参场景上是否有效。前置：G26;参数锁定。入口：主运行矩阵。

验收：主LLFF矩阵共96项，其中六个确认性场景为72项；先锁阈值；逐scene/seed审计且不删失败scene。实现：已有入口；运行：待服务器。证据：NR。

## G28｜scene级配对统计（P1）

问题：收益是否超出跨scene差异。前置：G27。入口：aggregate_results.py。

验收：每行绑定本scene/seed pair_id与passed audit；先均值seed再bootstrap scene；默认只取confirmatory；不足scene不报CI。实现：已有入口；运行：待服务器。证据：NR。

## G29｜无Track同初始化基线（P1）

问题：提升是否真正来自Track loss。前置：G21。入口：手动对称去除track项。

验收：保留source-only初始化与总预算。实现：现有参数可控，需独立配置；运行：待服务器。证据：NR。

## G30｜Self-render缓存控制（P1）

问题：额外伪监督本身是否产生效果。前置：G23。入口：run_stage.sh self-manifest; SelfRender; pair-self; pair-pseudo。

验收：同A0、32 cameras、50间隔、39 calls、source序列和loss权重；target严格为A0 render；不称Difix分支。实现：已有受控分支和审计入口；运行：待服务器。证据：NR。

## G31｜非生成增强控制（P1）

问题：是否只是锐化改善。前置：G23。入口：固定简单增强生成cache。

验收：相同target分辨率、预算；记录算法参数。实现：需补非生成cache驱动；运行：待服务器。证据：NR。

## G32｜独立DTU几何（P1）

问题：感知改善是否损害三维结构。前置：G28;G18。入口：DTU官方评估流程。

验收：校准/mask/单位/重建表示映射明确。实现：官方评估适配尚待完成；运行：待服务器。证据：NR。

## G33｜新视角与训练视角分离（P1）

问题：是否只提高source拟合。前置：G24;G25。入口：train/test分开记录。

验收：不把训练视角指标当NVS效果。实现：已有入口；运行：待服务器。证据：NR。

## G34｜GS/Difix/Real同域导出（P2）

问题：三类target比较是否可配对。前置：G24;G14。入口：run_stage.sh evidence-export-a1/evidence-export-b; evidence-cache-a1/evidence-cache-b; identity-manifest。

验收：同尺寸、同camera、reference只用source。实现：已有入口；运行：待服务器。证据：NR。

## G35｜DINO正确身份匹配（P2）

问题：source descriptor是否有效，并且B最终GS重建是否在同一paired support上具有强于A1的identity-consistent evidence。前置：G34;G19。入口：run_stage.sh identity-a1; identity-b; identity-report。

验收：真实DINO；记录token stride及输入尺寸；A1/B使用完全相同的paired support；主比较来自最终GS render，并报告failure-aware correct-query error、有效局部identity margin及全部失败计数。实现：已有入口；运行：待服务器。证据：NR。

## G36｜ProjectionOnly（P2）

问题：生成匹配是否优于现有几何。前置：G35。入口：paired_evidence_report.py。

验收：必须报告geometry-relative gain，不只matching误差。实现：已有入口；运行：待服务器。证据：NR。

## G37｜局部hard wrong身份（P2）

问题：是否真正利用Track identity。前置：G35。入口：evaluate_track_evidence.py。

验收：同局部窗口；无不同ID时记无效，不self冒充。实现：已有入口；运行：待服务器。证据：NR。

## G38｜全局轮换身份控制（P2）

问题：特定source query是否有作用。前置：G35。入口：global_shuffle字段。

验收：至少两条track；明确derangement构造。实现：已有入口；运行：待服务器。证据：NR。

## G39｜随机query控制（P2）

问题：是否由一般相似度偶然成功。前置：G35。入口：random_query字段。

验收：seed20260906；同候选support。实现：已有入口；运行：待服务器。证据：NR。

## G40｜均匀window控制（P2）

问题：是否仅因窗口中心接近真值。前置：G35。入口：uniform_query字段。

验收：uniform softargmax与ProjectionOnly同时报告。实现：已有入口；运行：待服务器。证据：NR。

## G41｜failure-aware配对（P2）

问题：失败是否被丢掉。前置：G35。入口：run_stage.sh identity-a1; identity-b; identity-report。

验收：PCK全分母、对角线惩罚、缺方法报错。实现：已有入口；运行：待服务器。证据：NR。

## G42｜GT-in-window位移诊断（P2）

问题：局部可恢复性如何。前置：G35。入口：shift 4/8/16字段。

验收：secondary oracle-conditioned，报告eligible数。实现：已有入口；运行：待服务器。证据：NR。

## G43｜非oracle位移鲁棒性（P2）

问题：真实扰动下是否有效。前置：G42。入口：不筛GT-inside全部评估。

验收：失败也入分母；勿借用proxy选样本。实现：需补无筛选导出分支；运行：待服务器。证据：NR。

## G44｜弱纹理/遮挡分层（P2）

问题：proxy是否仅覆盖容易区域。前置：G18;G35。入口：按人工标签/coverage分组。

验收：预先固定分层规则；保留空组。实现：分析脚本待扩展；运行：待服务器。证据：NR。

## G45｜错误身份margin与重建收益（P2）

问题：两个证据是否同向。前置：G28;G41。入口：场景级关联分析。

验收：只称关联，不当已证中介因果。实现：统计分析待补；运行：待服务器。证据：NR。

## G46｜Track权重（P3）

问题：0/.03/.1/.3敏感性。前置：G28。入口：独立配置对照。

验收：无Track使用专门baseline，不突破strict零权重guard。实现：需独立baseline配置；运行：待服务器。证据：NR。

## G47｜Pseudo权重（P3）

问题：0/.03/.1/.3的影响。前置：G28。入口：独立配置对照。

验收：0对应A1；其他统一source序列。实现：已有入口；运行：待服务器。证据：NR。

## G48｜搜索半径（P3）

问题：16/32/48是否改变结论。前置：G41。入口：--window-radii。

验收：主radius32锁定；其他为鲁棒性。实现：已有入口；运行：待服务器。证据：NR。

## G49｜温度（P3）

问题：0.03/0.07/0.15是否稳定。前置：G41。入口：--temperature。

验收：熵不是校准正确率。实现：已有入口；运行：待服务器。证据：NR。

## G50｜池大小与频率（P3）

问题：8/16/32/64与25/50/100。前置：G28。入口：非首轮controlled配置。

验收：调用数、coverage、总算力共同报告。实现：需独立对称配置；运行：待服务器。证据：NR。

## G51｜质量项消融（P3）

问题：长度/残差/无量纲不确定性各自贡献。前置：G28。入口：固定q组件的source-only构建。

验收：全部基于source；不得按heldout调q。实现：需补构建开关；运行：待服务器。证据：NR。

## G52｜关联碰撞率（P3）

问题：many-to-one是否降低有效约束。前置：G22。入口：现有日志＋阈值敏感性。

验收：记录唯一Gaussian数，不能只报track数。实现：已有入口；运行：待服务器。证据：NR。

## G53｜3/6/9视角（P3）

问题：结论是否仅限最稀疏情况。前置：G28。入口：VIEWS=3/6/9。

验收：使用不同source split hash；预算声明。实现：已有入口；运行：待服务器。证据：NR。

## G54｜源视图选择鲁棒性（P3）

问题：固定3图是否偶然好。前置：G28。入口：替代source名单。

验收：保持heldout冻结，不按测试效果选择图。实现：需明确替代split驱动；运行：待服务器。证据：NR。

## G55｜伪相机位置鲁棒性（P3）

问题：生成路径是否过于有利。前置：G28。入口：source-only路径配置。

验收：不可利用heldout RGB选择路径。实现：需补路径配置；运行：待服务器。证据：NR。

## G56｜恢复拓扑更新（P3）

问题：冻结拓扑结论能否外推。前置：G28。入口：双方对称densification。

验收：独立非首轮角色；checkpoint状态一致。实现：需独立对称配置；运行：待服务器。证据：NR。

## G57｜Mip360拓展（P3）

问题：无界场景适用性。前置：G28。入口：独立数据适配。

验收：先验证pseudo-camera合法，不直接跑全部。实现：数据协议适配待做；运行：待服务器。证据：NR。

## G58｜相关方法公平对照（P3）

问题：与Difix/HAD等差异是否真实。前置：G32。入口：各官方复现仓库。

验收：区分pose、预训练、budget与任务；不混文献分数。实现：外部基线未包含；运行：待服务器。证据：NR。

## G59｜训练/缓存/诊断时间（P4）

问题：总成本到底多少。前置：G28;G41。入口：同步CUDA计时。

验收：三部分分别统计，缓存不计为免费。实现：计时驱动待补；运行：待服务器。证据：NR。

## G60｜峰值显存与Gaussian数（P4）

问题：硬件负担与表示规模。前置：G59。入口：日志＋torch CUDA memory。

验收：显存reset/max与测量区间明确。实现：计时驱动待补；运行：待服务器。证据：NR。

## G61｜失败案例图库（P4）

问题：什么时候方法失败。前置：G28;G41。入口：固定选取规则。

验收：最大regression＋固定随机样本，不只挑好图。实现：需真实输出后制作；运行：待服务器。证据：NR。

## G62｜匿名重现包验收（P4）

问题：另一环境能否复现同结果。前置：全部核心实验。入口：干净服务器复跑。

验收：固定依赖、hash、命令、README；最终材料匿名。实现：待真实环境；运行：待服务器。证据：NR。
