# Historical validation snapshots

Every file in this directory is a **historical old-source snapshot** produced on 2026-09-06, before the current P0/P1/P2 repairs. The original logs and measured fixture numbers are retained for provenance only.

These files do not validate the current source tree. They must not be cited as evidence that current CPU checks, bootstrap assembly, CUDA gates, Fern training, Difix/DINOv2 inference, identity diagnostics, or independent geometry evaluation pass. Current runtime status is **NOT RUN / NOT VERIFIED**.

The current package has only undergone the static parsing and schema checks described in `../docs/变更与验证边界.md`; those checks are not runtime validation.
